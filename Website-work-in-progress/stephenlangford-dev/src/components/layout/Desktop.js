import React from 'react';
import Shortcut from '../ui/Shortcut';
import Window from '../ui/Window';

const Desktop = () => {
    return (
        <div>
            <Window section="summary" />
        </div>
    );
};

export default Desktop;
